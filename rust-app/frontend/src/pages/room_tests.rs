#[cfg(test)]
mod tests {
    #[test]
    fn test_room_component_compiles() {
        // This is a minimal test to ensure the component and its dependencies are correct.
        // True UI testing should be done in E2E.
        let val = 1;
        assert_eq!(val, 1);
    }
}
