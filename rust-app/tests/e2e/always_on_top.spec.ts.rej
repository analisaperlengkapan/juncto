--- always_on_top.spec.ts
+++ always_on_top.spec.ts
@@ -29,11 +29,11 @@

         // Test Video Toggle
         // In headless testing environments, accessing actual video hardware might fail, so we wait for the reaction
-        // await expect(videoBtn).toHaveText('📷');
-        // await videoBtn.click();
+        await expect(videoBtn).toHaveText('📷');
+        await videoBtn.click();

         // Wait for state to change. The toggle off removes the local stream which might take a fraction of a second.
-        // await expect(videoBtn).toHaveText('🚫', { timeout: 10000 });
+        await expect(videoBtn).toHaveText('🚫', { timeout: 10000 });

         // Test Leave Meeting
         await leaveBtn.click();
